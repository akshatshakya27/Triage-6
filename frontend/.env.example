# Browser requests use the Vite proxy by default. Restart Vite after changes.
API_PROXY_TARGET=http://127.0.0.1:8000
# For deployment with a separate backend, set its origin (without /api/v1).
VITE_API_BASE_URL=
